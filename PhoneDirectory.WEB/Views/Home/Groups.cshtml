@model IEnumerable<PhoneDirectory.WEB.Models.RegisterViewModel>

@{
    ViewData["Title"] = "Все занятия";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-sm-6 col-md-4 col-lg-3">
            <div class="card mb-3">
                Всем привет
            </div>
        </div>
    }
</div>
